unit Typez;

interface

uses
  SysUtils, Classes, Customers, Demands,pairs;

type
        CustArray = array of customer;
        ccustarray = array of custarray;
        realarray = array of array of real;
        rearray = array of real;
        intarray = array of integer;
        polar_coor =record
          cust:customer;
          ang:real;
        end;
        polararray = array of polar_coor;
        barray = array of boolean;


var
  n:integer;
  Q: integer;
  dist:realarray;
  dep:customer;
  dems:intarray;
  di:rearray;
  vlist: parray;
  v,p:integer;
  delta:real;


implementation

end.
