unit States;

interface

uses SysUtils, math, Customers, typez;

type
    state=Class

    private
     pcu_pos : customer;
     pdems : array of integer;
     pcu_cap : integer;

     published
     constructor Create(pcu_pos:customer; cu_cap: integer);
     property cu_pos : customer read pcu_pos write pcu_pos;
     property dems : array of integer read pdems write pdems;
     property cu_cap : integer read pcu_cap write pcu_cap;

    End;

implementation

constructor state.Create(pcu_pos:customer; cu_cap: integer);
var
i:integer;
pdems: array of integer;
begin
  self.cu_pos := pcu_pos;

  setlength(pdems,6); //Arreglar aca el tema del n
  pdems[0]:=0;
  for i := 1 to 6 - 1 do pdems[i]:=-8;
  self.dems := pdems;

  self.cu_cap := cu_cap;
end;
end.
